import 'package:flutter/material.dart';

// Minimal stub provider to satisfy imports during analysis.
// Replace with full implementation when available.
class PlantProvider extends ChangeNotifier {
  PlantProvider();
}
